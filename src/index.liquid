---
title: cobre 👋
layout: base.liquid
---
<div class="space-y-2">
  <div class="row mt-8 mb-4">
    <h1><strong>cobre</strong>  
      <span class="wave">👋</span>
    </h1>
  </div>
  <p>Just a guy with a passion for <strong>photography</strong> 📸 and <strong>tech</strong> 💻.</p>
    <p> Check out my <a class="arrow-external photos" href="https://cobre.photos/">photography portfolio</a>.</p>
</div>
<div class="flex flex-col gap-2 space-y-8 w-full justify-between xl:flex-row xl:space-y-0 xl:gap-8 xl:space-x-8">
  <div>
    <h2 class="text-zinc-400 mb-2 text-sm">Projects 🚧</h2>

    <div class="space-y-2">
      {%- for project in collections.projects -%}
        <div>
          <div class="row">
            <a href="{{ project.url }}">{{ project.data.title }}</a>
            <small class="text-zinc-500">{{ project.data.dateLabel }}</small>
          </div>
          <p class="text-zinc-400 ">{{ project.data.description }}</p>
        </div>
      {%- endfor -%}
    </div>
  </div>
  <div>
    <h2 class="text-zinc-400 mb-2 text-sm">Other things I'm working on 🛠️</h2>
    <div class="space-y-2">
      {%- for other in collections.others -%}
        <div>
          <div class="row">
            {%- assign link = other.data.url | default: other.url -%}
            {%- if other.data.url -%}
              {%- if link contains "cobre.photos" -%}
                <a class="arrow-external photos" href="{{ link }}">{{ other.data.title }}</a>
              {%- else -%}
                <a class="arrow-external" href="{{ link }}">{{ other.data.title }}</a>
              {%- endif -%}
            {%- else -%}
              {%- if link contains "cobre.photos" -%}
                <a class="photos" href="{{ link }}">{{ other.data.title }}</a>
              {%- else -%}
                <a href="{{ link }}">{{ other.data.title }}</a>
              {%- endif -%}
            {%- endif -%}
            <small class="text-zinc-500">{{ other.data.dateLabel }}</small>
          </div>
          <p class="text-zinc-400">{{ other.data.description }}</p>
        </div>
      {%- endfor -%}
    </div>
  </div>
</div>
<div>
  <h2 class="mb-2">Contact 📫</h2>
  <p>You can email me at
    <a href="mailto:cobre@cobre.dev" class="underline-email arrow-external">cobre@cobre.dev</a>, or connect with me through my
    <a href="/links/">links</a>.
  </p>
</div>
